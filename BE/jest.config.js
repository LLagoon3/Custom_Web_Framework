module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  verbose: true,
  // Optionally, add a path to the test files, if needed
  // testMatch: ['**/__tests__/**/*.ts?(x)', '**/?(*.)+(spec|test).ts?(x)']
};